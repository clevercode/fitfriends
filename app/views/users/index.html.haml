%h1 Listing users

%table
  %tr
    %th Name
    %th Email
    %th Start weight
    %th Goal weight
    %th Current weight
    %th Height
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.name
      %td= user.email
      %td= user.start_weight
      %td= user.goal_weight
      %td= user.current_weight
      %td= user.height
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New User', new_user_path
